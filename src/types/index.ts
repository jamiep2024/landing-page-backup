export interface LeadFormData {
  timestamp: string;
  name: string;
  phone: string;
  email: string;
  address?: string;
  service: string;
  urgency: string;
  message?: string;
  source: string;
  status: string;
  notes: string;
}
